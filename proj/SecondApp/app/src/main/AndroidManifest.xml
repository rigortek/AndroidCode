<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cw.secondapp">

<!--    广播接收者限被发送者限制制权限-->
<!--    <uses-permission android:name="com.cw.firstapp.permission.TEST"></uses-permission>-->

<!--    广播发送者被接收者限制权限-->
    <!--    <permission android:protectionLevel="signature" android:name="com.cw.permission.TEST" />-->
    <permission android:protectionLevel="normal" android:name="com.cw.permission.TEST" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>
    <uses-permission android:name="android.permission.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.WAKE_LOCK"></uses-permission>

    <permission android:name="com.cw.secondapp.permission.BINDSERVICE"
        android:protectionLevel="signatureOrSystem" />

    <application
        android:persistent="true"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
<!--        <activity android:name=".MainActivity">-->
<!--            <intent-filter>-->
<!--                <action android:name="android.intent.action.MAIN" />-->

<!--                <category android:name="android.intent.category.LAUNCHER" />-->
<!--            </intent-filter>-->
<!--        </activity>-->

        <activity android:name=".MediaTestActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 广播发送者被接收者限制权限 -->
        <receiver android:name=".MyBroadcastReceiver" android:exported="true"
            android:permission="com.cw.permission.TEST">
            <intent-filter>
<!--                <action android:name="com.cw.firstapp.action.TEST"></action>-->
                <action android:name="com.cw.secondapp.action.TEST"></action>
            </intent-filter>
        </receiver>
        
        <provider
            android:authorities="businessprovider.authorities"
            android:name=".BusinessContentProvider"
            android:exported="true">
        </provider>

        <service android:name=".MessengerService"
            android:exported="true"
            android:permission="com.cw.secondapp.permission.BINDSERVICE">
        </service>

    </application>

</manifest>